{
    "host": "localhost",
    "port": 3030,
    "public": "../public/",
    "paginate": {
        "default": 10,
        "max": 50
    },
    "nedb": "../data",
    "authentication": {
        "secret": "{{ secret }}",
        "strategies": [
            "jwt",
            "local"
        ],
        "path": "/authentication",
        "service": "users"
    }
}
